{
  "name": "blog-analytics",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "留心博客的自托管访问统计后端（Cloudflare Workers + D1）",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "db:count": "wrangler d1 execute web_analytics --remote --command \"SELECT COUNT(*) AS n FROM t_web_visitor\"",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.0.0"
  }
}
